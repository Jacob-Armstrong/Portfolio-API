[project]
name = "portfolio-api"
version = "0.1.0"
description = "An interactive API for my portfolio site!"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.12",
    "sqlmodel>=0.0.24",
]
